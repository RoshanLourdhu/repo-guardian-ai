spec_version: "0.1.0"

name: repo-guardian-ai
version: 1.0.0

description: AI-powered repository intelligence agent that analyzes code and generates trust-based insights.

model:
  preferred: groq:llama-3.3-70b-versatile
  config:
    temperature: 0.2
    max_tokens: 1000
    timeout: 30000

skills:
  - bug-analyzer
  - security-analyzer
  - improvement-engine
  - summary-generator

tags:
  - hackathon